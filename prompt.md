## Prompts

This file is generated at runtime to store the combined prompt for all users (`prompt-all-users.txt`) and per-user prompts (`prompt-*.txt`) under `output/`. Use them to inspect or reuse the requests sent to LMX.
